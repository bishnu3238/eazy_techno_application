enum ThemeType {dark, light}
enum NavBarParams { icon, label }
enum ConnectionType {wifi, mobile, none, }
enum Gender{male, female, others, unknown}
enum AuthenticationStatus { unknown, authenticated, unauthenticated }